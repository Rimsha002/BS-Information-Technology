#include <iostream>
#include "LSLL.h"
using namespace std;

Node<int>* FindCycleStart (Node<int>* head) 
{
 if(head == NULL)
 {
    cout << "There is no node";
    return;
 }
 if(head->next == NULL)
 {
    return head;
 }
if(head->next!= NULL)
{
    while(head->next!= NULL || (head->next))
    {
        

    }
}

    

}

int main()
{
    LS
   return 0;
}